services:
  backend:
    build: ./backend
    container_name: patchwell-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    env_file:
      - .env
